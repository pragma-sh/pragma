# Local environment for the marketing site (apps/www). Copy to `.env` and fill in.
# In deployments this is set on the Vercel project instead, for all three environments.

# Server-side key for the /support form's splitforms inbox.
# Obtain at https://splitforms.com/dashboard
SPLIT_FORMS_ACCESS_KEY=ADD HERE
