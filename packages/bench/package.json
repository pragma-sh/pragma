{
  "name": "@pragma/bench",
  "version": "0.0.0",
  "private": true,
  "description": "Terminal latency benchmark for Pragma. Dual TS + Rust, like @pragma/constants: the Rust half measures the transport, the TypeScript half measures the parser and the frontend policy.",
  "type": "module",
  "exports": {
    "./report": "./src/report.ts"
  },
  "scripts": {
    "bench": "bun src/index.ts",
    "typecheck": "tsc --noEmit",
    "test": "bun --bun vitest run",
    "lint": "oxlint ."
  },
  "devDependencies": {
    "@types/node": "^24.12.3",
    "@xterm/headless": "6.1.0-beta.292",
    "typescript": "^6.0.3",
    "vitest": "^4.1.8"
  }
}
